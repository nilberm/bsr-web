import Header from "@/components/Header";

export default function ExpenseLimit() {
  return (
    <main className="flex flex-col w-full ">
      <Header backBtn={false} pageName="Expense Limit" />
      ExpenseLimit
    </main>
  );
}
